# Replay tests

This suite owns deterministic transcript, virtual-clock, fragmentation, cancellation, reconnect, and checkpoint replay checks.

Replay tests must not use a live Discord account or external network service. Inputs and expected outputs are fixture files with stable digests.
