# Integration tests

This suite verifies complete library boundaries against controlled local HTTP and WebSocket fixture servers.

Integration tests remain offline from Discord, use synthetic credentials only, enforce bounded queues and response limits, and verify clean cancellation and shutdown.
